---
layout: post
title:  "回溯法"
date:	2021-11-11 20:30:35 +0800
categories: jekyll update
---
## 对于求最大值回溯法的剪枝

### 题目如下
>羽毛球队有男女运动员各n人. 给定2个n*n矩阵P和Q. P[i][j]是男运动员i与女运动员j配混合双打的男运动员竞赛优势; Q[i][j]是女运动员i与男运动员j配混合双打的女运动员竞赛优势. 由于技术配合和心理状态等各种因素影响, P[i][j]不一定等于Q[j][i]. 男运动员i和女运动员j配对的竞赛优势是P[i][j]*Q[j][i].设计一个算法, 计算男女运动员最佳配对法, 使得各组男女双方竞赛优势的总和达到最大.
>数据输入:第1行有一个正整数n(1<=n<=12), 接下来2n行是P和Q
>结果输出:最佳配对的各组男女双方竞赛优势总和 

>sample input:
>
>```
>3
>10 2 3
>2 3 4
>3 4 5
>2 2 2
>3 5 3
>4 5 1
>```
>
>sample output:
>
>```
>52
>```

这是一道深度优先搜索的题目，其中关键是这道题要对最大值进行剪枝。

对最大值剪枝的方法：
> 先求出对于每个男队员与所有女队员的竞赛优势的最大值，保存在maxsum数组中.
> 在进行深搜时，计算还没有进行匹配的男运动员理论上可以获得的最大竞赛优势，再加上已经匹配了
> 的男运动员的竞赛优势。若两者之和仍然小于之前已经计算出的结果，则没有必要继续算下去，
> 直接return.

代码如下：

```c
#include <cstdio>
#include <cstdlib>
#include <cstring>
#include <algorithm>

using namespace std;
const int maxn=20;

int a[maxn][maxn];
int b[maxn][maxn];
int vis[maxn];
int data[maxn][maxn];
int maxsum[maxn];
int n;
int ret=-1;
int sum=0;

void calc(int start)
{
	if(start==n)
	{
		ret=max(ret,sum);
		return;
	}
	int cnt=0;
	for(int i=start;i<n;i++)
		cnt+=maxsum[i];
	if(sum+cnt<ret)
		return;
	for(int i=0;i<n;i++)
	{
		if(vis[i]==0)
		{
			vis[i]=1;
			sum+=a[start][i]*b[i][start];
			calc(start+1);
			sum-=a[start][i]*b[i][start];
			vis[i]=0;
		}
	}
}

int main(void)
{
//	freopen("in.txt","r",stdin);
	scanf("%d",&n);
	for(int i=0;i<n;i++)
		for(int j=0;j<n;j++)
			scanf("%d",&a[i][j]);
	for(int i=0;i<n;i++)
		for(int j=0;j<n;j++)
			scanf("%d",&b[i][j]);
	for(int i=0;i<n;i++)
	{
		for(int j=0;j<n;j++)
		{
			data[i][j]=a[i][j]*b[j][i];
			maxsum[i]=max(maxsum[i],data[i][j]);
		}
	}
	calc(0);
	printf("%d\n",ret);
	return 0;
}
```
